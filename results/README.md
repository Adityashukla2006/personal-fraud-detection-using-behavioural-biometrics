# results

Experimental output from both tracks: the offline research result and the measured cloud result.

- `figures/`, plots.
- `tables/`, numeric result tables.

The headline figure is **poisoning resistance against false rejection rate under genuine drift**,
comparing three update policies: static profile, naive update and guarded update. It is the result
that answers the primary research objective O5.

Supporting output: the ROC curve and EER against the published benchmark baseline, profile
displacement over successive poisoning sessions, detection rate per attack class, and the latency
and cost per thousand sessions figures from the deployment.

All values: TODO, pending the experiments.

Owner: Arushi Tiwari (23BIT0181) for detection and poisoning output, Aditya Shukla (23BIT0250) for
latency and cost output.
